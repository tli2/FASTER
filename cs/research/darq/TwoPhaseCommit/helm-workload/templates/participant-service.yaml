{{- range .Values.participants }}
apiVersion: v1
kind: Service
metadata:
  labels:
    service: participant{{ .num }}
  name: participant{{ .num }} 
spec:
  ports:
    - name: "15721"
      port: 15721
      targetPort: 15721
  selector:
    service: participant{{ .num }}
---
{{- end }}